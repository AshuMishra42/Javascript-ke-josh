const naam ="Ankush"
const repocount = 5

console.log(naam + repocount + "value" );

// Previously we used this type of technique to con-catenate  to two or more variables

console.log(`Hello my name is ${naam} and my repocount is ${repocount}`);

// Here is the new trick to con-catenate two variables. 
// we can declare string in another type

const gameName = new String("Anshu");
console.log(gameName);

//  Here by using keyword new we are using object
